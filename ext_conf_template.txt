# cat=basic//10; type=boolean; label= Search if no pid: If view has no pid then searches are conducted in all records of cache_pages. It can be very time consuming so it is disabled by default.
searchIfNoPid = 0
# cat=basic//100; type=integer; label= Priority: If there is more than one extension that uses hook of ttnewscache then you may set priority they are processed.
ttnewscache_priority = 100
